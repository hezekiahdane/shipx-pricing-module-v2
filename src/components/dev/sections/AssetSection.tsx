'use client';

// Task 10 will wire this to context (DOM scan + show-more).
// For now, no-prop stub that renders nothing until assets are available.
export function AssetSection() {
  return null;
}
